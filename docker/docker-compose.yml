version: '3.8'

services:
  # Backend application (commented out for development)
  # app:
  #   build:
  #     context: ..
  #     dockerfile: docker/Dockerfile
  #   container_name: mms-backend    
  #   environment:
  #     - SPRING_PROFILES_ACTIVE=docker
  #     - SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/mms
  #     - SPRING_DATASOURCE_USERNAME=admin
  #     - SPRING_DATASOURCE_PASSWORD=adminmms
  #     - STORAGE_TYPE=local
  #     - STORAGE_LOCAL_BASE_PATH=/app/buckets
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     - db
  #   networks:
  #     - mms-network
  #   volumes:
  #     - ../buckets:/app/buckets  # Mount local storage directory
  #   restart: unless-stopped

  db:
    image: postgres:16-alpine
    container_name: mms-postgres
    environment:
      - POSTGRES_DB=mms
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=adminmms
    volumes:
      - ./volumes/postgres_data:/var/lib/postgresql/data
      - ../database/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    ports:
      - "5432:5432"
    networks:
      - mms-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d mms"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

networks:
  mms-network:
    driver: bridge

